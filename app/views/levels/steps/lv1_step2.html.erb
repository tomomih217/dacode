<% content_for(:title, t('levels.steps.lv1_step2')) %>
<div class='container col-lg-8 offset-lg-2'>
  <div class='row'>
    <div class='border my-3'>
      <p>クイズを埋めたけど・・・あれ？これおかしくないか？<br>あっ、取扱説明書に続きがある！<br>「最後にメソッドを使ってボタンを変換すること」？<br>下にはメソッドが書いてあるけど、どういう意味？</p>
      <h5>正しく設定しなおそう。</h5>
    </div>
    <div class='d-flex mt-3'>
      <%= image_tag 'blue_button.png', size: '200x130' %>
      <div class='ms-5'>
        <p>設定</p>
        <h5>HTTPメソッド：DELETE</h5>
        <h3>RESPONSE</h3>
        <p>を送信しクソコードを破壊する。</p>
      </div>
    </div>
    <div>
      <label name='answer'>使用するメソッド名</label>
      <input type='text', name='answer'>
      <input type='button', value='実行', class='btn btn-sm btn-success'>
    </div>
    <div>
      <div class='border border-dark rounded-3 p-3 my-3'>
        <div class='quiz-method'>def change_1(response)<br>  adds = [0, 0, -3, 8, -10, 7, 3, x]<br>  response.map.with_index{|x, index| x = x + adds[index]}<br>end</div>
      </div>
      <div class='border border-dark rounded-3 p-3 my-3'>
        <div class='quiz-method'>def change_2(response)<br>  adds = [0, 0, -2, 5, -10, 5, 1, x]<br>  response.map.with_index{|x, index| x = x + adds[index]}<br>end</div>
      </div>
      <div class='border border-dark rounded-3 p-3 my-3'>
        <div class='quiz-method'>def change_3(response)<br>  adds = [0, 1, -4, 5, -9, 6, 2, x]<br>  response.map.with_index{|x, index| x = x + adds[index]}<br>end</div>
      </div>
    </div>
  </div>
</div>