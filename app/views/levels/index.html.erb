<div class='container'>
  <div class='row'>
    <div class='my-5'>
      <div class='col-sm-10 offset-sm-1'>
        <% @levels.each do |level| %>
        <h3><%= link_to level.name, level_path(level) %></h3>
        <% end %>
      </div>
    </div>
  </div>
</div>